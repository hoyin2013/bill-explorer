# 2026-08-11

- 为 bill-explorer 执行 macOS 打包：`npm run electron:build`，成功生成 `release/BillExplorer-1.0.0-arm64.dmg`（98MB, arm64）。
  - 环境前置：Electron 二进制缺失，先运行 `node node_modules/electron/install.js` 下载 v28.3.3 到 `dist`。
  - 打包未做代码签名（无 Developer ID 证书），dmg 可生成，但首次打开会触发 Gatekeeper 拦截，需在「系统设置 → 隐私与安全性」中手动允许。

- 生成 `BUILD.md` 打包文档（环境要求/构建架构/npm scripts/打包步骤/常见问题）。
- 功能改动（渲染进程）：
  1. 预览窗口不再碍事：`ResultList.tsx` 新增「悬停预览 开/关」开关（默认**关**，localStorage `bill-explorer:preview-enabled` 持久化），预览卡片头部加 ✕ 关闭按钮。
  2. Excel 式最近输入：`MemoPanel.tsx` 新增列级「最近输入」记忆（文本列 no/date/name/unit/person/remark），聚焦空格 120ms 后自动弹出下拉（Alt+↓ 手动），↑↓+Enter 选择，输入自动过滤；历史存 localStorage `bill-explorer:memo-history:v1`（每列 20 条），跨文件/跨会话复用；Esc 关面板前与 Ctrl+Enter 保存前会把已输入值兜底记入历史。
- 修复启动问题（本次发现）：
  - 主进程/预加载改用 `from 'electron'`（原 `electron/main`、`electron/renderer` 子路径需 Electron≥29，本机是 28.3.3，无法启动）。
  - 本机环境有 `ELECTRON_RUN_AS_NODE=1`（非 shell 配置，疑似执行环境注入），会让 Electron 以纯 Node 运行、显示不了窗口；`run-dev.sh` 已加 `export ELECTRON_RUN_AS_NODE=` 防御。手动运行需 `ELECTRON_RUN_AS_NODE= NODE_OPTIONS= npx electron .`（NODE_OPTIONS 里的 --use-system-ca 也会报错）。
  - 冒烟/自动化测试启动参数：`ELECTRON_RUN_AS_NODE= NODE_OPTIONS= npx electron . --no-sandbox --remote-debugging-port=9333`（--no-sandbox 仅限本执行环境，用户终端不需要）。
- 验证方式：CDP（remote-debugging-port + Node22 内置 WebSocket）对真实 Electron 页面做端到端测试；React 受控输入需用 Input.insertText 真实键入，`new Event('input')` 合成事件不触发 onChange。

- GitHub Actions 云端打包 Windows：新建 `.github/workflows/build-windows.yml`（windows-latest + Node20 + npm ci + `npm run electron:build:win`，`CSC_IDENTITY_AUTO_DISCOVERY=false`；手动 workflow_dispatch + push tag v* 触发，tag 触发时 softprops/action-gh-release 自动发 Release）。BUILD.md 新增第 9 章。注意：仓库 remote 目前是 **Gitee**（gitee.com/hoyin2020/xiujuan.git），用 GitHub Actions 前需先推 GitHub（git remote add github ...）。

