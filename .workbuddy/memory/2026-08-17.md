# 2026-08-17 工作日志

## 🐞 三个账单表格交互 bug 修复（日期变数字 / 双击不弹提示 / 选中行不删整行）
- **Bug1 日期填入变 45874（裸序列号）**：自动补全在 `beforeSub` 拦截 `SetRangeValuesCommand` 写入候选时，日期列只写了 `v:序列号, t:NUMBER`，没带日期样式。加载的日期单元格有单元格级 `s:'bill-date'`（univerAdapter `rowsToWorkbookData` 给每个日期格显式套），所以显示正常；但自动补全写入时只有列级 `columnData[1].s`（本版本不生效）→ 退化成裸数字。**修复**：`acController.beforeSub` 中当 `acceptCol===DATE_COL_INDEX && 数字` 时给 `cmd.params.value` 加 `s: DATE_STYLE`（='bill-date'，univerAdapter 已 `export`）。`univerAdapter.ts` 把 `const DATE_STYLE` 改为 `export const`。
- **Bug2 双击空单元格不弹提示列表**：`acController.recompute` 在文本为空时直接 `closePopup()`，且 UniverSheet 本地 `computeSuggestions` 空 partial 返回 `[]`。**修复**：recompute 空文本也调 `computeItems`（不再早退）；UniverSheet `computeSuggestions` 空 partial 改为返回本列全部候选（非空才做 startsWith→includes 过滤）。双击空格进入编辑即出全部可选项。
- **Bug3 选中整行按 Delete 只清内容不删行（三次修复·终版）**：前两次（window 冒泡/捕获 keydown 监听 + 命令拦截）都失败。根因定位：Univer 的 `ShortcutService` 用 `fromGlobalEvent('keydown', …, {capture:true})` 把快捷键监听挂在 **window 捕获阶段且注册早于本应用**，所以本应用任何 window keydown 监听都抢不过它；而 `ClearSelectionContentCommand`（`sheet.command.clear-selection-content`，Delete/Backspace 派发）的 `beforeCommandExecuted` 无法取消命令、且快捷键派发时 `params` 为 `undefined`（重新赋值 `cmd.params` 传不到 handler，置空 `ranges` 只对带参情况有效）——所以命令拦截路线也不可靠。**终版方案（正确 Univer 姿势）**：注册自定义命令 `bill-explorer.command.delete-selected-rows`（`FWorksheet.deleteRows` 删整行 + 调 `autoAmountRows` 行号集合 + markDirty/scheduleAutoSave）+ 用 `IShortcutService.registerShortcut` 注册 **高优先级(100)快捷键** 绑定 `KeyCode.DELETE` 与 `KeyCode.BACKSPACE`，`preconditions` 判定「非编辑态 + startCol===0 && endCol>=COL_COUNT-1 + startRow>=1」（即整行选中）。整行选中时本快捷键优先级高于 Univer 原生清除快捷键、先命中并消费按键 → 干净只删行；单格/区域删除、编辑态删字符保持原生。注意点：`univer.__getInjector()` 取 `ICommandService`/`IShortcutService`；`preconditions` 内含 activeElement 检查避免编辑态误删整行；命令与快捷键在卸载时 dispose。
- **验证**：`npx tsc -p tsconfig.json --noEmit` 通过；`npm run build:renderer` 构建成功。GUI 整行删除需本机 Electron 实测确认（高优先级快捷键接管是关键，区别于抢 keydown 事件）。

## 自动补全触发行为对齐 WPS/Excel（用户二次明确：单击/进编辑不弹，只输入才弹 + 候选不含表头）
- **需求变更**：上一版「双击空单元格出全部候选」被用户推翻，改为与 WPS/Excel 对齐——**只有真正键入内容时才弹提示**；单击/选中单元格、或进入编辑但还没敲字（即使单元格已有内容）都不弹。
- **实现（`acController.ts`）**：新增 `hasInput` / `initialText` 状态。`attachEditor` 时记录 `initialText = readInitialText()` 并 `hasInput=false`；`input$` 订阅回调 `handleText(content, true)` → 置位 `hasInput`（若文本仍等于 `initialText` 视为初始、不置位）；`recompute` 仅当 `hasInput && t 非空` 才计算并展示候选，否则 `closePopup()`。`endEdit` / `accept` / `currentEditCell$` 新编辑会话均复位 `hasInput=false`。由此：单击仅选中→不进编辑→无弹；双击/进编辑但未敲字→`hasInput=false`→不弹；键入字符→`input$`→弹且按已输入过滤。
- **候选排除表头（`UniverSheet.tsx` `computeSuggestions`）**：`getColumnValuesWithRows(col).filter(x => x.row >= 1 && x.row < currentRow)` —— 排除 row 0（表头是列名如「货品名称」「日期」，非数据值，原本会被当成候选，尤其日期列会冒出「日期」二字）。
- **验证**：`npx tsc -p tsconfig.json --noEmit` 通过；`npm run build:renderer` 构建成功；`npm run test:ac` 10 单测全过。GUI 行为需本机 Electron 实测。

## 自动补全（acController.ts）重设计定稿 + 真实 Univer e2e 验证通过
- 旧版反复失败根因：DOM 轮询命中 Univer 屏幕外零尺寸「公式栏/隐藏占位」编辑器，永远读不到文本、不提示。
- 新版彻底事件驱动：`IEditorBridgeService.currentEditCell$`（进入/退出编辑）+ `IEditorService.getEditor(id).input$`（实时文本）+ 官方 `position` 反查真实单元格编辑器矩形定位弹层。
- **回填（写入候选）唯一可靠注入点 = `commandService.beforeCommandExecuted` 拦截 `SetRangeValuesCommand`，把其 `params.value` 换成候选（字符串/数字 + 正确 `CellValueType`）**。落盘由 Univer 自己用 `editCellState` 的正确 row 完成。
- 关键坑（已逐一验证）：
  1. `currentEditCell$.row` 比真实 0 基行 **+1 偏移**；绝不能用它定位落盘格（写错行）。
  2. keydown 里 `replaceText` 后，Univer 原生提交读的是另一份快照（时序问题）→ 写不进候选。故放弃 replaceText 回填。
  3. Univer 的 Enter keydown 监听注册在 `window` 捕获、且**早于本控制器**，会先提交「旧文本」。因此键盘路径不能靠 keydown 顺序抢先。
- **订单无关方案**：再注册 `beforeCommandExecuted` 拦截 `SetCellEditVisibleOperation`（visible:false + keycode 13/9）。当其执行前且弹层仍开 → 把高亮候选 `armAccept` 为 `pendingAccept`；随后该命令内部 `_submitEdit` 发的 `SetRangeValuesCommand` 被第一个拦截器换值落盘。键盘/鼠标两条路径最终都汇到 `pendingAccept`+`SetRangeValuesCommand` 拦截器。
- `endEdit`（visible$ 触发）**不能**清空 `pendingAccept`：因 `_exitInput`（先）→ `_submitEdit`（后）顺序，拦截器在 endEdit 之后才消费；清空交给拦截器与 1s `pendingTimer` 兜底。
- 验证：`npm run test:ac` 10 单测全过；生产 `npm run build:renderer` 通过；`/tmp/ac-test/run-e2e-real.mjs`（puppeteer + 真实 Univer + `ac-e2e-real.html`）两条全过——键盘 Enter→`"苹果干"`、鼠标点选→`46244`(日期序列号)。
- 配套改动：`UniverSheet.commitAc` 改为仅 `pushHistory`（真正的单元格写入交还 Univer 原生提交）；`host.commit(item, col)` 去掉 row 形参。
- 已存技能 `univer-autocomplete`（用户级）复用此模式。

## 修复：真实 App「输入过程完全不提示」（attachAutocomplete 挂载时机错误）
- 现象：e2e（`ac-e2e-real.html`，attach 在 `createWorkbook` **之后**）全过，但真实 App 打字零提示。
- 根因：`UniverSheet.tsx` 把 `attachAutocomplete` 放在 effect 顶层、`createWorkbook` **之前**（异步 IIFE 里才 `createWorkbook`）。而 Univer 的 `IEditorBridgeService`（id `univer.sheet-editor-bridge.service`）要等首个工作表实例化（`createWorkbook`）后才完成 DI 注册；提前 `injector.get(IEditorBridgeService)` 抛 `QuantityCheckError: [redi]: Expect 1 dependency item(s) ... but get 0`，被 `attachAutocomplete` 的 try/catch 吞掉、静默降级 `noOpController` → 只 `console.warn('[AC] 注入编辑服务失败…but get 0')`，全程无提示。
- 复现/定位：起 `ac-react-test.tsx`（React+StrictMode 镜像 UniverSheet）用 puppeteer 单击+打字 → 控制台两次 `[AC] 注入编辑服务失败…but get 0`、`__acUi` undefined、DOM 无 `.ac-item`；把 attach 挪到 `createWorkbook` 之后 → 弹层出现（候选 苹果干/苹果梨/苹果汁/苹果）。注：StrictMode 双挂载不是本 bug 主因（双次都失败，因为是「时机」而非「实例」问题）。
- 修复：`UniverSheet.tsx` 把 `attachAutocomplete` 调用从 effect 顶层移到异步 IIFE 内 `univerAPI.createWorkbook(data)` 之后（源码已改 + 注释说明缘由）。
- 验证：`npx tsc --noEmit` 通过；`npm run test:ac` 10 全过；`npm run build:renderer` 构建成功（dist 已更新为含修复的 `main-BQndc7yO.js`）。
- 已回写技能 `univer-autocomplete`：新增「坑 0 —— 必须先 createWorkbook 再 attachAutocomplete，否则完全不提示」+ 核心结论一节。

## 修复：编辑区点候选「要点两次才消失」
- 现象（用户实测两次）：点第一下能带入候选值，但提示弹层不消失；再点一下才消失。
- 第一版修复（只给 mousedown 加 stopPropagation）**无效**——说明漏的不是 mousedown 本身。
- 真正根因：候选 `onMouseDown` 上就 `accept()`（关弹层+提交），弹层在「mousedown→mouseup」整段手势**中途被移除**，随后的 mouseup/click 落到下方 Univer 画布；Univer 把它解读为"点到某格→重新进入该格编辑"，`currentEditCell$` 再次触发、按该格内容重算候选、弹层重开（第一次点只带入值、弹层顽固不消失；再点才消是因为那次碰巧落在空单元格上无候选）。
- 最终修复（`UniverSheet.tsx` + 同步 `ac-e2e-real.html`）：
  - 候选项 `onMouseDown` 只 `e.preventDefault()`（保住编辑器焦点、不丢文本，**先不 accept/不关弹层**）；
  - 候选项 `onClick` 才 `e.preventDefault(); e.stopPropagation(); accept(it)`（accept 挪到 click，弹层陪完整个手势，mouseup/click 始终落在弹层内被吞掉，画布收不到事件→杜绝重开）；
  - `.ac-popup` 容器挂 `onMouseDown`/`onClick` 的 `stopPropagation` 兜底。
- 经验：`accept` 的落盘路径（executeCommand→beforeCommandExecuted 拦截 SetRangeValuesCommand 换值）不依赖 DOM 冒泡，把 accept 从 mousedown 挪到 click 不影响回填。
- 验证：`npx tsc --noEmit -p tsconfig.json` 通过。环境无浏览器自动化，无法 puppeteer 实跑；请本机点一次候选确认弹层一次消失即生效。
- 已回写技能 `univer-autocomplete`：鼠标点选一条改为「accept 必须放 onClick、onMouseDown 只 preventDefault」，并写清真正根因。

## 🔧 FINAL：双击关闭 bug 真正定位 + 修复（前两次修法都漏了「层二」）
- 现象（用户实测）：编辑区点候选，第一下只带入值、提示不消失；点第二下才消失。
- **真正根因 = 两层叠加，缺一层都复发：**
  - **层一（DOM 手势）**：`onMouseDown` 上就 `accept()` 会让弹层在 mousedown→mouseup 中途被移除，mouseup/click 漏到 Univer 画布被解读为「重进编辑」→ 重开。修：accept 挪到 `onClick`，`onMouseDown` 只 `preventDefault`，容器 `stopPropagation` 兜底。
  - **层二（命令同步重发，决定性、前两次都栽在这）**：即使 accept 在 click 上，`executeCommand(SetCellEditVisibleOperation,{visible:false})` 退出链会**同步**触发 `endEdit`→`currentEditCell$` 再 emit 同一格→`recompute` 带旧文本把弹层又开出来。
- **决定性修复（acController.ts `accept`）**：在 `executeCommand` **之前**抓 `guardRow=editRow/guardCol=editCol` 并**立即**置位「重开抑制」`acceptGuardRow/Col`，起 300ms 定时器清零；再调命令。`recompute` 开头若 `acceptGuardRow===row && acceptGuardCol===col` 则 `closePopup()` 并 return。**必须在命令前置位**——命令同步跑完退出链（含 recompute），等返回后才置位会被先跑过的 recompute 绕过。退出 keycode 传 `0`（不移动/不重进）。
- **本机已用 puppeteer（系统 Chrome + puppeteer-core，Vite:5174）真实跑通**：真实键盘输入 "苹" 自然弹出候选 → 点一次 → `window.__ac.open===false` 且目标格 `==="苹果"`，无需第二下。验证脚本 `/tmp/acdiag/diag.js` + 测试页 `ac-e2e-real.html`。
- 已清理 acController.ts 内的 `[TRACE] console.error` 调试日志（生产不应有）。
- 顺带按用户要求**移除 UniverSheet.tsx 手动「保存」按钮**（项目已有连续自动保存 + Ctrl+S 快捷键，按钮冗余）；仅删可见按钮，Ctrl+S 保留。
- 技能 `univer-autocomplete` 已同步：鼠标点选条目改为「两层根因 + 决定性修复 = 命令前置位重开抑制 guard」，并标注「必须用 puppeteer 真实点一次候选验证」。

## 🐞 修复：悬停单元格弹「sheets-ui.info.error / forceStringInfo」裸键
- 现象：鼠标悬停含「数字存成文本」的单元格，弹窗标题/正文显示裸键名 `sheets-ui.info.error`、`sheets-ui.info.forceStringInfo`（而非中文）。
- 根因：Univer `sheets-ui` 的「数字以文本形式存储」告警（`_initCellAlertPopup`，`sheets-ui/lib/index.js` ~L24971）用 `t("sheets-ui.info.error"/"forceStringInfo")` 取文案，但 `sheets-ui` 语言包（含 preset 合并进来的那份 `preset-sheets-core/locales/zh-CN`）的 `info` 块**从未定义**这两个键；真正的翻译只在死键 `sheets-numfmt-ui.info.*` 命名空间下。取不到 → 渲染裸键。
- 修复（方案 A·关掉告警）：`UniverSheet.tsx` `createUniver` 之后加 `univer.__getInjector().get(IConfigService).setConfig('sheets-ui.config', { disableForceStringAlert: true })`；新增 `import { IConfigService } from '@univerjs/core'`。账单录入器无此 Excel 式提示需求，且货品名/备注常含数字文本会误报。
- 备选方案 B（保留提示、补翻译）：给 `locales` 的 `sheets-ui.info` 补 `error:'错误'`、`forceStringInfo:'以文本形式存储的数字'`（本次未采用）。

## 🐞 修复：选中行按退格/Delete 不删整行（row-delete 快捷键从不触发）
- 现象（用户）：选中整行敲退格键，期望删除整行，实际没删（退格进编辑态 / Delete 只清内容）。上一轮 Bug3 的「高优先级快捷键接管」其实从未生效。
- **真正根因**：删除命令与 `isFullRowSelected()` 里都用 `univerRef.current?.getActiveSheet()?.worksheet` 取工作表。运行时 `FUniver.getActiveSheet()` 返回的是 `{workbook, worksheet}` 包装对象，`.worksheet` 是 **core `Worksheet`（模型）**——它**没有 `deleteRows()` 方法**（删除是 facade 命令，只在 `FWorksheet` 上）。于是 `isFullRowSelected()` 取 range 后判定通过、快捷键被高优先级接管，但 handler 里 `ws.deleteRows()` 抛 `TypeError` 被 catch 吞掉 → 返回 false → 行没删；退格路径还让原生 `EditorDeleteLeftShortcutInActive`（binding BACKSPACE，`whenSheetEditorFocused`）抢先进编辑态。
- 关键验证：运行时 `getActiveWorkbook().getActiveSheet()` 才返回真正的 **`FWorksheet`**（facade），它既有 `getActiveRange()` 也有 `deleteRows()`。注意 `FWorksheet` 只有受保护 `_worksheet`、无公开 `.worksheet` getter，所以 `.worksheet` 永远 undefined——之前之所以没崩是因为可选链静默降级 + catch 兜底，表现为「不删行」。
- 修复：`UniverSheet.tsx` 把 handler 与 `isFullRowSelected` 两处的 `…getActiveSheet()?.worksheet` 改为 `…getActiveWorkbook()?.getActiveSheet()`（拿到 FWorksheet）。快捷键注册（DELETE/BACKSPACE 双绑、priority 100、preconditions=整行选中）保持不变。
- 验证：`npx tsc -p tsconfig.json --noEmit` 通过（exit 0）。
- 注意：文件里另有 4 处 `univerAPI.getActiveSheet()?.worksheet`（L103/191/465/672）仍在用 core `Worksheet`，因那些功能只调 core 模型方法、未涉及 `deleteRows`，目前工作正常，本次未动；若日后要统一走 facade 可一并改。
- 遗留：本机 Electron GUI 实测「选中行 + Delete/退格 → 整行删除 + 下方上移 + 自动金额集合同步」仍待用户点一次确认。

## 🔧 强化：选中行删除改为直接复用右键菜单「删除选中行」命令
- 需求（用户）：选中一行按 Delete/退格时，直接执行与右键菜单「删除选中行」完全相同的操作（而非自实现删除）。
- 根因/定位：上一轮虽已让整行删除能用，但走的是 `FWorksheet.deleteRows()` 自实现路径。用户要求与右键菜单 100% 一致。
- 关键发现：右键菜单「删除选中行」实际调用 **`RemoveRowCommand`（`sheet.command.remove-row`）**，参数只需 `{ range: IRange }`（单位/子表 id 来自 `Partial<ISheetCommandSharedParams>`，可选）；`RemoveRowByRangeCommand`（`sheet.command.remove-row-by-range`）则需 `unitId/subUnitId`，非右键所用。
- 修复（`UniverSheet.tsx` 删除命令 handler）：整行选中命中时，先按选区算 `startRow/endRow` 同步 `autoAmountRows` 集合（删除区间丢弃、下方整体 `-delCount`），再 `injector.get(ICommandService).executeCommand('sheet.command.remove-row', { range: {startRow, startColumn:0, endRow, endColumn: COL_COUNT-1} })` 转发给 Univer 内置命令——选区处理/合并单元格/撤销栈均与右键菜单同一条代码路径。`isFullRowSelected` 的 `startRow>=1 + 整列` 判定（procondition）继续保护表头行、单格/区域删除与编辑态删字符保持原生。
- 验证：`npx tsc -p tsconfig.json --noEmit` 通过（exit 0）。GUI 实测「选中行 + Delete/退格 == 右键删除选中行」仍待用户点一次确认。

## 🎯 自动补全下拉框定位改为「紧贴输入框正下方」（对齐 WPS/Excel）
- 需求（用户）：提示框要像 WPS/Excel 一样，紧贴正在编辑的「输入框」正下方（左缘对齐、宽度对齐）。
- 旧实现隐患：`findEditorRect()` 用 `document.querySelectorAll("[data-u-comp='editor']")` 取所有编辑器 DOM、再「距离单元格坐标最近」猜测。当 Univer 公式栏（顶部）编辑器存在时，会干扰；且首次输入时编辑器 DOM 常还是 0 尺寸 → 回退到 `posRef` 坐标（基准易错）→ 弹层落在错误位置、之后不更新。
- 关键 API：`IEditor` 实例（经 `editorService.getEditor(editEditorId)` 取得）直接提供 `getBoundingClientRect(): DOMRect` 与 `get editorDOM`。`editEditorId` 来自 `currentEditCell$.editorUnitId`，是当前编辑单元唯一且准确的标识。
- 修复（`acController.ts` 的 `findEditorRect`）：
  1. **首选**：`editorService.getEditor(editEditorId).getBoundingClientRect()`，取视口坐标的编辑器矩形 → `{ left: r.left, top: r.top + r.height + 2, width: r.width }`。直接对应「输入框正下方、左对齐、同宽」，彻底避开公式栏/占位壳干扰，且每次 input 重算 pos 会跟随。
  2. **回退**（getEditor 拿不到有效矩形时）：仍扫描真实 `[data-u-comp='editor']`，但多候选时排除位于容器顶部的公式栏编辑器（`r.top < bt + 40` 惩罚），取纵向最贴近单元格者。
- 配合 `.ac-popup { position: fixed; z-index: 2000 }`（index.css L2129），inline left/top 即视口坐标，与 getBoundingClientRect 一致。
- 验证：`npx tsc -p tsconfig.json --noEmit` 通过（exit 0）；`npm run build:renderer` 构建中。真实落点需本机 Electron 进单元格编辑+输入，目视确认弹层紧贴输入框正下方。
