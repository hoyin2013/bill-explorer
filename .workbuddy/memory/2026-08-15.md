# 2026-08-15

- 需求落地（小票识图面板）：
  1. 移除 AI 识别结果表的「序号」列（删 `ImageWindow` 的 `RESULT_COLS` 中 `no` 项；主网格/备忘录的 序号 不动）。
  2. 图片旋转自动保存 + 统一方向：新增 `get-image-rotation`/`set-image-rotation` IPC（electron-store 键 `imageRotation`，默认 0）；`ImageWindow` 加 `defaultRotateRef`/`rotateRef`，`loadPreview` 套用默认方向，`rotateBy` 在旋转时持久化，`resetRotation` 复位到默认方向而非强制 0。主进程 IPC 在 `main.ts`，preload 与 `types.ts` 的 `ElectronAPI` 同步新增两个方法。
- `npm run build` 通过（renderer tsc + vite、main/preload esbuild 均绿）。
