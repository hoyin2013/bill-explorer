# 2026-08-15

- 框选「相邻行被误染蓝」修复：根因是拖拽 `onMove` 每帧 `setActive` 触发 `scrollIntoView({block:'nearest'})`，当光标在可视区边缘时滚动→静止光标落到相邻行→下一帧 `cellAtPoint` 读到新行→选区被越界吞入上一行/下一行（跨行框选时表现为"没选的相邻行尾部也变蓝"）。修复：`active` 的滚动 effect 在 `rangeDragRef`/`fillDragRef` 存在时直接 return（拖拽中不自动滚）；并把边缘滚动改到 `onMove` 里——仅当 `me.clientY` 越过 `.sheet-scroll` 容器上/下边界（`cr.top`/`cr.bottom`）才 `scrollTop ∓= 28`，视口内拖动不滚动，避免正反馈失控，同时仍支持拖到边缘把选区延伸到视口外。`onUp` 还原激活格到锚点时用 `suppressScrollRef` 抑制那一下 `scrollIntoView`。改动文件 `src/renderer/components/SheetGrid.tsx`，`npm run build` 通过。

- 需求落地（小票识图面板）：
  1. 移除 AI 识别结果表的「序号」列（删 `ImageWindow` 的 `RESULT_COLS` 中 `no` 项；主网格/备忘录的 序号 不动）。
  2. 图片旋转自动保存 + 统一方向：新增 `get-image-rotation`/`set-image-rotation` IPC（electron-store 键 `imageRotation`，默认 0）；`ImageWindow` 加 `defaultRotateRef`/`rotateRef`，`loadPreview` 套用默认方向，`rotateBy` 在旋转时持久化，`resetRotation` 复位到默认方向而非强制 0。主进程 IPC 在 `main.ts`，preload 与 `types.ts` 的 `ElectronAPI` 同步新增两个方法。
- `npm run build` 通过（renderer tsc + vite、main/preload esbuild 均绿）。

## 框选/选区视觉与头部布局修复（下午）
- **去掉"十字高亮"**：原 `.row-active td` / `.col-active` 会给当前激活格所在的**整行/整列**染淡蓝底，导致"没选中的数据也变蓝"。已移除这两处背景，并删除对应 JSX 的 `row-active`/`col-active` class 赋值与 `bandOff` 派生变量。现在**只有选区（`.cell-range`）本身填充蓝色，激活格仅保留 `.td-active` 蓝色描边**——与 Excel 一致。
- **头部小屏布局**：`.memo-header` / `.memo-header-actions` 加 `flex-wrap: wrap`，`.memo-file` 加 `flex:1 1 auto; min-width:0`，窗口变窄时按钮自动换行而非溢出错乱。

# 2026-08-15（续）— 账单网格框选交互对齐 Excel

- 修正「跨行框选会多带上一行/整行看起来被选中」问题，并让框选手感与 Excel 一致：
  1. 框选命中改用**几何定位**（`cellAtPoint`/`rowIndexAtY`，按 `tbody.rows` 各行/列 bounding rect 计算），取代 `document.elementFromPoint` + `closest('[data-r]')`。原因：旧方式会被填充柄、行号、单元格内边距干扰，且整行/整列高亮带（`row-active`/`col-active`）在框选时仍显示，造成"整行被选中"的错觉。
  2. 存在多选区域（矩形框选 >1 格，或整行选择 `selRows`）时，**隐藏**整行/整列高亮带，只保留选区 `cell-range` 与活动格边框（`bandOff` 派生值控制）。
  3. 拖拽释放后**活动格回到按下起点**（Excel 习惯：活动单元格=按下处），而非选区左上角。
  4. 新增**行号拖拽连选多行**：`onRowNumMouseDown`（Ctrl/Shift 仍走 onClick 单选/多选）；`onRowNumMouseDown` 与 `onCellMouseDown` 共用 `rangeDragRef`（加 `trs`/`rowMode` 字段），`tbody.rows` 在 mousedown 时缓存一次。
- 改动文件：`src/renderer/components/SheetGrid.tsx`（框选/行号拖拽/高亮带）。`npm run build` 通过。

- 主网格（SheetGrid）交互改动：
  1. 打开账单文件时**不再自动进入编辑态**（移除 load 成功的 `setEditing(true)`），光标仍落在序号列，避免打开就弹出序号编辑框。其余显式操作（识别回填/插入行/复制行/+行按钮）仍保持自动编辑不变。
  2. 序号列新增 **Excel 式拖拽填充序列**：激活序号格右下角渲染 `.fill-handle`，`onFillHandleDown` 拖动按种子整数生成 ±1 递增序列（1,2,3…），拖动区间 `.fill-range` 高亮，支持撤销；种子非整数时退化为整列复制。仅序号列（`noIdx`）生效，避免数量/单价/金额误填充。样式在 `index.css`。
  3. 右键菜单新增 **「序号自动编号」**：`autoNumber(start)` 从右键行向下给序号列填 1,2,3…，末尾行 = 自底向上第一个非空行（不给空白预备行编号）；本行已有整数则续编、否则从 1 起；支持撤销。菜单项放在「复制此行到下方」与「删除本行」之间。
  4. **矩形框选（Excel 式拖拽选数据）**：新增 `selRange` 状态（`{r1,c1,r2,c2}`，锚点+拉伸格）。单元格 `.cell` 的 `onMouseDown` 调 `onCellMouseDown`：左键非编辑态下，移动超 4px 进入拖拽，用 `document.elementFromPoint` + `data-r/data-c`（标在 `<td>` 上）算终点格，`setSelRange` + `setActive`；松手没拖动则 `setSelRange(null)`（单击），拖动则把锚点设到选区左上角并吞掉尾随 click（`suppressClickRef`）。选区单元格加 `.cell-range` 淡蓝高亮（`index.css`）。
     - 选择优先级统一为：**矩形框选 > 整行选择(selRows) > 单格**，封装 `selBlockTSV()`（块→TSV+尺寸）与 `clearSelectionBlock()`（清空内容不删行）。
     - 接入：原生 `onCopy`/`onCut`/`onPaste`、键盘 `Delete/Backspace`→`clearSelectionBlock`、`Ctrl+X`→块复制+清空、输入字符（框选时清空选区后在左上角进入编辑）、右键菜单「复制/剪切/清空内容」、`pasteFromClipboard`（粘贴到选区左上角）。`nav` 方向键与 `openMenu`（右键选区外）会清除框选。
     - 与现有交互无冲突：填充柄 `onMouseDown` 已 `stopPropagation`；行号整行选择独立；双击编辑/`onClick` 单选在 `suppressClickRef` 守卫下正常工作。
- 本次新增修复：
  1. **小票阅读顺序排序**：`ImageWindow` 新增模块级 `readingOrderIndices(boxes)`——先按中心 y 排序、再按纵向间隙聚成「行」、行内按 x 从左到右。`detectOnly` 拿到 `res.boxes/res.tickets` 后按该顺序重排 boxes/tickets 并重置 `index` 为 1 基新序号。缩略图、检测框编号、「填入全部」、点「下一张」均变为人类阅读顺序（左上→右→下一行）。
  2. **Excel 日期差一天+带时分秒**：根因是 `toExcelDate` 用 `new Date(y,mo-1,d)`（本地午夜），被 ExcelJS 按 UTC 偏移成带小数序列号（实测 2026-08-15 → 序列号 46248.666 → 真实 Excel 显示 2026-8-14 且带 16:00）。改为 `new Date(Date.UTC(y,mo-1,d))`（序列号 46249 → 2026-8-15，整数无时间），并容忍 `2026/8/15`、尾随时间、`2026年8月15日`、`20260815` 等写法（只取年月日）；`fmtDateLocal` 读回改 UTC getter，往返一致。已用 node 跑 ExcelJS 往返验证多格式。
  - `npm run build` 通过（renderer tsc + vite、main/preload esbuild 均绿）。

## 识图：单张"横七竖八" + 顺序错 + 总览序号（晚上）
- **单张小票"横七竖八"根因**：主进程 `detectTickets` 调 `runDetect`（detection.ts，走 ONNX `detect_onnx.cjs`）时传 `noRotate:false`，于是 `detect_onnx.cjs` 对**每张裁剪图独立做 `bestRotation` 自动旋转**（按"行投影方差"猜 0/90/180/270）。该启发式对接近方形/文字少/排版均匀的小票常猜错角度——总览整图是用户手动转好的（CSS rotate + 旋转后的预览图），但每张 crop 被各自乱转，单张放大时才暴露。修复：`detection.ts` 三处（ONNX 主路径、ONNX detectFromFile、Python 回退 `--no-rotate`）全部改 `noRotate:true`。现在 crop = 用户在总览里转好的那张图的原始矩形切片，单张视图 `rotate(singleRotate=0)` 即正确方向。单张视图的 ↺/↻ 仍为手动微调逃生口（`singleRotate` 仅本地显示、不回写 crop，识别仍用原 crop——已知限制）。
- **阅读顺序错**：原 `readingOrderIndices`（ImageWindow.tsx）按"上一行底部纵向间隙"聚类，某张特别高/偏低的票与下一行纵向交叠时会把下一行错并进来。改为"按当前行中心 y 做行归属"：先按中心 y 升序，再用**行平均中心**（行内各票 cy 均值）做阈值判断，`tol=max(20, 中位高度*0.6)`，行内按中心 x 从左到右。更稳健。
- **总览序号**：缩略图条（`#i+1`）与检测框（`#i+1`）本就带序号，且依赖 `tickets` 排列顺序；顺序修对后序号即按"从左到右、从上到下"正确排列（ticket.index 也在 detectOnly 重排时重置为 1 基）。保留"录入结果表不显示序号列"的既有约定（对录入无意义），序号仅体现在总览的逐张缩略图/检测框上。
- `npm run build` 通过（renderer tsc + vite、main/preload esbuild 均绿）。

## 框选吸附：避免垂直拖动误选相邻列（次日早晨）
- **问题反馈**：用户从「块」（货品名称）列向下框选三行时，实际高亮横向扩展到了单价/数量等列，只想选中单列。
- **原因**：框选 `onMove` 用 `cellAtPoint` 严格按鼠标当前位置计算终点列，拖动时手稍有横向抖动（或鼠标轨迹带横向分量）就会进入相邻列，形成矩形扩展。
- **修复**：在 `onMove` 中加入**起始单元格吸附**——只有当鼠标离开起始单元格超过一半列宽时，才允许列变化；超过一半行高时，才允许行变化。这样纯垂直拖动即使有小幅度水平抖动，也只会选中起始列；同理纯水平拖动不会误跨行。大幅度斜向拖动仍可按矩形扩展。改动 `src/renderer/components/SheetGrid.tsx`，`npm run build` 通过。

## 「填入」改为落到主窗口光标激活行（非追加末尾）
- **需求**：点识图窗口的「填入本张/填入全部/填入当前录入」时，结果应填到主窗口网格里光标当前激活的那一行，而不是永远追加到表格末尾。
- **链路**：`ImageWindow.applyRows` → `api.applyToMain` → 主进程 `image:apply-rows` → 主窗口渲染进程 `apply-recognized-rows` → `SheetGrid.applyRecognizedRows(rows, append)`。
- **改动**（`src/renderer/components/SheetGrid.tsx`）：① `apply-recognized-rows` 事件回调里把恒为 `true` 的入参改为 `false`；`append=false` 时 `applyRecognizedRows` 以 `activeRef.current.r`（主窗口网格激活行）为起点（`append=true` 才用 `bottomEntryRow` 末尾）。② 同一函数末尾把 `setActive/setInputRow` 的起点由 `start` 推进到 `start + rows.length`，确保连续多次「填入」落在新一批空行、不覆盖刚填内容。
- 注：典型用法是先在账单录入网格点选目标行（激活光标），再到识图窗口点「填入」。打开 Excel 时 `active` 默认在底部空行，故默认行为与以前一致；只有用户主动移动光标后才体现差异。`npm run build` 通过。

## 保存时「中间空行被删除」修复（用户按月用 2 个空行作分隔）
- **问题反馈**：用户在账本里每个月数据之间有意留 2 行空行作分隔，但保存后这些空行消失了（重开文件即消失）。
- **根因**：① 前端 `SheetGrid.handleSave` 只 `pop` 尾部空行，中间空行确实传给了 `saveSheet`；② 但 `saveSheet` 把空行写成 `cell.value = null`，而 **ExcelJS 不会把"所有单元格为空"的行写入 .xlsx 文件**（稀疏存储），空行被丢弃；③ 下次 `loadSheet` 用 `ws.eachRow` 遍历，空行本就不在文件里，自然读不回来。所以"保存→重开"后空行永久消失。
- **修复**（`src/main/excel-memo.ts`）：给"数据区内的空行"写入一个**不可见占位符**，让 ExcelJS 把该行写入文件，读回时再还原成真实空行。
  - `saveSheet`：先算 `lastContent`（最后一个有实质内容行的下标）；写回时若某行全空且 `i <= lastContent`（数据区内，非末尾未使用行），则 `row.values = []` 清空后写 `row.getCell(1).value = '\u200b'`（零宽空格）。末尾空行仍按原逻辑剥离。
  - `loadSheet`：读回时若某行 **仅由占位符构成**（`arr.every(t => t === PLACEHOLDER || t.trim() === '')`，PLACEHOLDER=`'\u200b'`），则还原为 `HEADER.map(() => '')` 全空行；真正的脏空行（`arr.every(t => t.trim() === '')` 且不含占位）仍跳过。
  - **关键坑**：占位符**不能用 `\u00a0`（不间断空格）**——Node 22 的 `String.prototype.trim()` 会把 `\u00a0` 当 Unicode 空白移除，导致读回时 `arr.every(t => t.trim()==='')` 为真、占位行被误判为脏空行跳过。必须用 **`\u200b`**（零宽空格，Unicode 格式字符 Cf，不属于空白，trim 不移除），显示不可见但能骗过 eachRow 与 trim 判断。
- **验证**：写独立 round-trip 脚本（真实 ExcelJS）模拟 save→load，6 行（含中间 2 空行）保存重开后读回仍为 6 行、中间两行序号为空 —— PASS。验证后删除脚本。
- `npm run build` 通过。
